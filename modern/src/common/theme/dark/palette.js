import {
  amber, grey, green, blue, red, common,
} from '@mui/material/colors';

const colors = {
  white: common.black,
  background: grey[500],
  primary: blue[900],
  secondary: green[500],
  positive: green[500],
  medium: amber[700],
  negative: red[500],
  neutral: grey[500],
  geometry: '#3bb2d0',
};

export default {
  mode: 'dark',
  colors,
};
